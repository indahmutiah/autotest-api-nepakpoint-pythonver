# API Autotest Nepak Point 2 with Playwright Python

## 📝 Overview

This is a simple API testing project using Playwright Python. The project is designed to test the API endpoints of the nepak-point-api.indahmutiah.com. The project uses pytest as the testing framework and Playwright as the automation tool.

## 💻 Installation
Make venv before installing dependencies. Then, install the dependencies using pip:

```bash
pip install -r requirements.txt
```

## 🧪 Testing

To run the tests, execute the following command in your terminal:

```bash
pytest -V tc-products.py
```
